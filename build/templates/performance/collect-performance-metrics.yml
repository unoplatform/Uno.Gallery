parameters:
  TargetFolder: ''
  ScriptPath: ''

- bash: |
    chmod +x "${{ parameters.ScriptPath }}"
    BUILD_ID=$(Build.BuildId)
    SOURCE_VERSION=$(Build.SourceVersion)
    "${{ parameters.ScriptPath }}" "${{ parameters.TargetFolder }}"
  displayName: Generate metrics JSON
  env:
    BUILD_ID: $(Build.BuildId)
    SOURCE_VERSION: $(Build.SourceVersion)
